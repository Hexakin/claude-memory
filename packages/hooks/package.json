{
  "name": "@claude-memory/hooks",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "claude-memory-hook": "dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format esm --outDir dist",
    "dev": "tsup src/cli.ts --format esm --outDir dist --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@claude-memory/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
